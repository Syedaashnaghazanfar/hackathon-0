# Obsidian Vault Configuration
VAULT_PATH=./AI_Employee_Vault

# Watcher Configuration
WATCH_FOLDER=./watch_folder
WATCH_MODE=events

# Optional: Dedupe State File Location (outside vault)
DEDUPE_STATE_FILE=./.dedupe_state.json
